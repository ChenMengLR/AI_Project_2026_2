# AI_Project_2026_2 workspace rules

This is the single shared local workspace for the course:
`C:\Users\31797\Documents\ChatGPT\作业 3\AI_Project_2026_2`.

Before changing anything:

1. Read `HANDOFF.md`, `README.md`, and every relevant file in `docs/status/`.
2. Run `Get-Location` and `git status --short --branch`.
3. Stop and report if the current directory is not the project root above.
4. Keep each week's work in its own sibling folder named `weekNN_topic`.
5. Reuse the root `.venv`; never create a second virtual environment in a week folder.
6. Preserve previous weeks unless the user explicitly requests a fix there.
7. Keep secrets in local ignored `.env` files only. Never print, commit, upload, or screenshot API keys.
8. Before committing, inspect the staged file list and confirm no `.env` is present.
9. After a milestone, update `docs/status/WeekNN_STATUS.md` with evidence, tests, and remaining work.
10. One window writes to this working tree at a time; use a separate Git worktree for parallel edits.

The current personal Week 3 entry point is `week03_rule_chatbot/app.py`. Its
course summary is `docs/Week03_Rule_QA_Summary.md`; its current status is
`docs/status/Week03_STATUS.md`.
